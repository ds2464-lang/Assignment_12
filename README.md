Fast API Calculator with user endpoints and BREAD

Instruction on Running Tests Locally

1. Install dependencies:

pip install -r requirements.txt

pip install pytest

pip install pytest-cov

2. Run tests using pytest:

pytest or pytest -v

DockerHub Repository Link:

https://hub.docker.com/repository/docker/domsil12/assignment12/general
